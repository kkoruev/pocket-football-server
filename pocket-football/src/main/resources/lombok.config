#Needed after lombok version bump to 1.18.10, because as of version 1.16.20 lombok stopped generation @ConstructorProperties
#To avoid adding @NoArgsContructors to entities, this configuration property should be used.
lombok.anyConstructor.addConstructorProperties = true
#Needed for lombok exclusion of unwanted methods. It will add the following annotation
#@lombok.Generated - the generated by Lombok getters, setters, equals, hashcode, toString, etc
#will be skipped in the coverage analysis.
lombok.addLombokGeneratedAnnotation = true
#Indicates that this is the root directory
config.stopBubbling = true
# Copy the Qualifier annotation from the instance variables to the constructor
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Value
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
